**Quick Sort:O(nlogn)**
def partition(left,right,li=[list]):
  temp=len[left]
  while left<right:
    while left<right and li[right]>=temp:
      right-=1
      li[left]=li[right]
    while left<right and li[left]>=temp:
      left+=1
      li[right]=li[left]
  li[left]=temp
  return li[left]

def Quick_sort(left,right,li):
  left=o
  right=len(li)-1
  if left<right:
    mid=partition(left,right,li)
    Quick_sort(left,mid-1,li)
    Quick_sort(mid+1,right,li)


**
    
    
    
  
