
**27:28 ms**
class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        c=0
        for i in range(len(nums)):
            if nums[i]!=val:
                nums[c]=nums[i]
                c+=1
        return c
        
 ***26:52ms***
 if not nums:
            return 0
        c=0
        for i in range(len(nums)):
            if nums[i]!=nums[c]:
                c+=1
                nums[c]=nums[i]
        return c+1
  
  
            
